# TFT Match Crawler — configuration
# Copy this file to .env and fill in your values.

# ---------------------------------------------------------------------------
# API
# ---------------------------------------------------------------------------
RIOT_API_KEY=RGAPI-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ---------------------------------------------------------------------------
# Collection
# ---------------------------------------------------------------------------
# Platform routing value for league/summoner endpoints.
# Options: na1, euw1, eun1, kr, jp1, br1, la1, la2, tr1, ru,
#          oc1, ph2, sg2, th2, tw2, vn2
PLATFORM=na1

# Comma-separated list of leagues to collect.
# Options: challenger, grandmaster, master
LEAGUES=challenger

# Riot queue identifier
QUEUE=RANKED_TFT

# Recent matches to fetch per player (1-200).
MATCHES_PER_PLAYER=50

# ---------------------------------------------------------------------------
# Time window (optional)
# Restrict match IDs to a time range. Values are Unix timestamps in milliseconds.
# Leave blank to apply no filter on that side.
# TFT Patch Schedule: https://support-teamfighttactics.riotgames.com/hc/en-us/articles/37127675562387-Patch-Schedule-Teamfight-Tactics
#
# TODO: AS OF 13/02/2026, the startTime flag is not respected by the API.
# Current implementation will continuously pull match history until startTime is met.
# ---------------------------------------------------------------------------
START_TIME=
END_TIME=

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
DB_PATH=tft_data.db

# ---------------------------------------------------------------------------
# Rate limit (in seconds)
# ---------------------------------------------------------------------------
REQUEST_DELAY=1.2

# ---------------------------------------------------------------------------
# Logging
# Options: DEBUG, INFO, WARNING, ERROR
# ---------------------------------------------------------------------------
LOG_LEVEL=INFO
